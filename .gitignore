# зависимости и сборка
node_modules/
*.log
*.tmp

# --- НИКОГДА не коммитить: доступы и персональные данные ---
# ВАЖНО: git не понимает комментарии в конце строки с шаблоном,
# поэтому каждое правило — на отдельной строке.

# ssh-ключи, пароли, инвентарь серверов
server-access/
.env
.env.*
!.env.example
*.pem
*.key
*.ppk
id_rsa*
id_ed25519*
*password*.txt
*secret*
authorized_keys
known_hosts

# заявки с форм — персональные данные клиентов
data/leads.json

# служебные файлы разбора прайса
data/_source/check.txt
data/_source/_enc.txt
data/*.tmp

# рабочая папка для присланных материалов, оригиналы переносятся в public/img/source
another/
